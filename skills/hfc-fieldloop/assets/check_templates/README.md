# Check templates (M1–M13)
#
# Authority for live runs: ../../scripts/lib/run_checks.R (+ media.R for M12,
# geo_timezone.R for M5's timezone resolution)
# These files are copyable skeletons for project checks/ after setup.
# They assume `ds` (data), `roles` (list), and helpers from scripts/lib/utils.R.
#
# Map: see ../../references/check_modules.md
